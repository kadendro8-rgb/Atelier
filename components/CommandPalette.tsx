"use client";

/**
 * Global command palette (Cmd+K).
 *
 * TODO(v2-section-6): fuzzy search across actions, plans, builders, and
 * clients using `cmdk`. See docs/v2-spec.md §6.
 */
export function CommandPalette() {
  return null;
}
