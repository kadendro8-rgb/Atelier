# factory/

Daily standup notes for the Atelier agent factory. One file per day:
`standup-YYYY-MM-DD.md`.

Each entry records, per active worker: what shipped since yesterday, what's
shipping today, what's blocked. See `docs/factory.md` for the operating model.
